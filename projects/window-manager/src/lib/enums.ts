export enum EVENTS {
  CLOSE = 'close',
  OPEN = 'open',
  FOCUS = 'focus',
  BLUR = 'blur',
  RESTORE = 'restore',
  MAXIMIZE = 'maximize',
  SHOW = 'show',
  MINIMIZE = 'minimize',
}

export enum Position {
  LEFT = 'left',
  RIGHT = 'right',
  TOP = 'top',
  BOTTOM = 'bottom',
  CENTER = 'center',
}
